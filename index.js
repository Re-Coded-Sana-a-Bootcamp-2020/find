const nums = [-3, -2, 0, 4];

let firstPositive;

// A sample solution using for loops might look like this.
// for (let n of nums) {
//   if (n > 0) {
//     firstPositive = n;
//     break;
//   }
// }

// Create the same result as above with find.
